# Streamlit 1.57.0 has reported OAuth state/cookie regressions on callback.
# Pin to 1.56.0 for stable st.login() behavior.
streamlit==1.56.0
pandas
requests
pyzipper
ftfy
Authlib>=1.3.2
supabase>=2.0.0
